#pragma once
/* Generated by netkit AOT compiler (lowered) — static kernel call chain */

#include "arena.hpp"

#include <cstddef>
#include <cstdint>

namespace netkit::aot::mnist_mlp {

inline constexpr const char* kName = "mnist_mlp";
inline constexpr const char* kNetwork = "mlp";
inline constexpr bool kLowered = true;
inline constexpr std::uint32_t kInputElements = 784u;
inline constexpr std::uint32_t kOutputElements = 10u;
inline constexpr std::uint32_t kInputShape[] = {1, 784};
inline constexpr std::uint32_t kInputRank = 2u;
inline constexpr std::size_t kScratchFloats = 128u;
inline constexpr std::size_t kArenaBytesAfterLoad = 407104u;
inline constexpr std::size_t kArenaBytesAfterForward = 407104u;
inline constexpr std::size_t kArenaBytesRecommended = 456000u;

inline bool InitArena(Arena& arena, void* memory, std::size_t capacity)
{
    if (!memory || capacity < kArenaBytesRecommended)
        return false;
    arena.init(memory, capacity);
    return true;
}

class Model {
public:
    Model() = default;

    bool load(Arena& arena);
    bool forward(Arena& arena, const float* input, float* output) const;
    [[nodiscard]] bool isLoaded() const { return loaded_; }

private:
    bool loaded_ = false;
};

}  // namespace netkit::aot::mnist_mlp
